export default function App() {

  return (
    <div style={{ maxWidth: '1200px', margin: '0 auto', padding: '40px 24px' }}>
      <header style={{ marginBottom: '40px', display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
        <div>
          <h1>TidyGPT Local Sandbox</h1>
          <p style={{ color: 'var(--text-muted)' }}>Load the extension in Chrome for the full Dashboard.</p>
        </div>
      </header>
      <main className="glass-panel">
        <p>This is the local-web harness. The primary UI is now in the Extension Dashboard.</p>
      </main>
    </div>
  );
}
