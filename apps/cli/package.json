{
  "name": "@tidygpt/cli",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "bin": {
    "chat-sweeper": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "dev": "tsup src/index.ts --format cjs --dts --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tidygpt/shared": "*",
    "@tidygpt/core": "*",
    "commander": "^11.1.0",
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.3.0"
  }
}
